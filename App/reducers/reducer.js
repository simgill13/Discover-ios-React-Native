import {
 
} from '../actions/action';

const initialState = {
    displayName: 'Sim --test from reducer'   
}

export default (state = initialState, action) => {
  switch(action.type) {
   
    default:
        return state;
  }
}







